/*
------------------
-- COLLECTIONS  --
------------------
-- A collection is an array like homogeneous single-dimensional structure, which holds a set of elements of similar data type.

COLLECTION TYPES
-- Associative array
    - NON-persistent Unbounded
    - cannot be created in database schema
-- Nested tables
    - Persistent Unbounded
    - unbounded collection has no limit on the number of elements
-- Varaible array
    - Persistent Bounded
    - bounded collection has fixed number of elements
*/
